1.代理模式原理
  一个类代表另一个类的功能。这种类型的设计模式属于结构型模式。
  在代理模式中，我们创建具有现有对象的对象，以便向外界提供功能接口。

2.Java RMI
   RMI远程方法调用是计算机之间通过网络实现对象调用的一种通讯机制。

3.几种常见的代理模式
  虚拟代理：为创建开销大的对象提供代理服务。真正的对象在创建前和创建中时，由虚拟代理扮演替身
  动态代理: 运行是动态的创建代理类对象，并将方法调用转发到指定类。
  保护代理:


4.代理模式的关键点
   代理模式目的是对目标对象访问的控制和管理。不增加新的功能

  