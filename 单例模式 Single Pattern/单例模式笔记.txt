1.经典单例模式
  目的：确保一个类最多只有一个实例，并提供一个全局访问点。
  多线程中保持一致性。

2.关键点：
  私有化构造方法，对外提供全局访问的返回单一对象的方法

3.饱汉式和饿汉式

4.饱汉式在多线程中的安全性。

5.饱汉式的线程安全。双重检查加锁法
   给属性加volatile关键字，双重检验。
    关键代码：
   if(obj == null) {
    synchronized(obj.class) {
       if(obj == null ){
              obj = new Obj();
        }
      }  
   }